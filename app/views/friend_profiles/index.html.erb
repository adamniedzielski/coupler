<h1>Your friends profiles</h1>

<ul>
	<% @friend_profiles.each do |friend_profile| %>
		<li>
			<%= friend_profile.name %> <%= link_to "Edit", edit_friend_profile_path(friend_profile) %>
    </li>
	<% end %>
</ul>

<%= link_to "Create new profile", new_friend_profile_path %>
